<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:newx="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="_CameraFilterVM"
            type="com.newx.camera.vm.CameraFilterVM"></variable>
        <import type="me.tatarka.bindingcollectionadapter2.LayoutManagers" />

    </data>

    <android.support.constraint.ConstraintLayout
        android:id="@+id/layout_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white">

        <android.support.v7.widget.RecyclerView
            android:id="@+id/list_filter"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            newx:itemBinding="@{_CameraFilterVM.mCameraFilterItem}"
            newx:items="@{_CameraFilterVM.mCameraFilterList}"
            newx:layoutManager="@{LayoutManagers.grid(2)}">

        </android.support.v7.widget.RecyclerView>

    </android.support.constraint.ConstraintLayout>
</layout>