<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:muv="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="itemLocalVideoVM"
            type="com.newx.muv.vm.ItemLocalVideoVM"></variable>
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/dp_200"
        android:layout_margin="@dimen/dp_2"
        android:background="@color/primary"
        muv:onClickCommand="@{itemLocalVideoVM.itemClick}">

        <ImageView
            muv:srcBitmap="@{itemLocalVideoVM.mThumb}"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitCenter"/>


        <TextView
            android:layout_width="wrap_content"
            android:layout_height="@dimen/dp_16"
            android:layout_margin="@dimen/dp_2"
            android:textColor="@color/primary_light"
            android:text="@{String.valueOf(itemLocalVideoVM.mEntity.duration)}"
            android:textSize="@dimen/font_smaller"
            muv:layout_constraintBottom_toBottomOf="parent"
            muv:layout_constraintEnd_toEndOf="parent"/>

    </android.support.constraint.ConstraintLayout>
</layout>