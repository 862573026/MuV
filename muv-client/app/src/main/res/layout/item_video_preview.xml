<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>
        <variable
            name="itemVideoPre"
            type="com.newx.muv.vm.ItemVideoPreVM"></variable>
    </data>

    <android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dp_200"
        android:layout_margin="@dimen/dp_2"
        app:onClickCommand="@{itemVideoPre.itemClick}"
        app:backgroundUrl="@{itemVideoPre.mEntity.previewUrl}">

        <com.newx.base.widget.CircleImageView
            android:id="@+id/iv_ownerIcon"
            android:layout_width="@dimen/dp_24"
            android:layout_height="@dimen/dp_24"
            android:layout_margin="@dimen/dp_2"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:srcCompat="@mipmap/ic_launcher"
            app:imageUrl="@{itemVideoPre.mEntity.ownerIcon}"/>

        <TextView
            android:id="@+id/tv_ownerName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/dp_2"
            android:text="@{itemVideoPre.mEntity.ownerName}"
            android:textSize="@dimen/font_smaller"
            app:layout_constraintBottom_toBottomOf="@+id/iv_ownerIcon"
            app:layout_constraintStart_toEndOf="@+id/iv_ownerIcon"
            app:layout_constraintTop_toTopOf="@+id/iv_ownerIcon" />

        <ImageView
            android:id="@+id/iv_like"
            android:layout_width="@dimen/dp_16"
            android:layout_height="@dimen/dp_16"
            app:layout_constraintBottom_toBottomOf="@+id/iv_ownerIcon"
            app:layout_constraintEnd_toStartOf="@+id/tv_likeNum"
            app:layout_constraintTop_toTopOf="@+id/iv_ownerIcon"
            app:srcCompat="@mipmap/icon_like" />

        <TextView
            android:id="@+id/tv_likeNum"
            android:layout_width="wrap_content"
            android:layout_height="@dimen/dp_16"
            android:layout_margin="@dimen/dp_2"
            android:text="@{String.valueOf(itemVideoPre.mEntity.likeNum)}"
            android:textSize="@dimen/font_smaller"
            app:layout_constraintBottom_toBottomOf="@+id/iv_ownerIcon"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@+id/iv_ownerIcon" />

    </android.support.constraint.ConstraintLayout>
</layout>