<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.newx.muv">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:name="com.newx.muv.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/TransparentAppTheme">

        <activity
            android:name="com.newx.muv.view.init.InitActivity"
            android:theme="@style/Translucent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.newx.muv.view.temp.TempActivity"></activity>


        <activity
            android:name=".view.tab.TabActivity"
            android:launchMode="singleTask"></activity>

        <activity android:name="com.newx.muv.base.page.activity.NxSwipeBackActivity"></activity>

        <activity android:name=".base.page.activity.NxFragmentActivity"></activity>

        <activity android:name="com.newx.base.utils.utilcode.util.PermissionUtils$PermissionActivity"></activity>

        <activity android:name=".view.test.NettyTestActivity"></activity>

        <activity android:name=".view.TestActivity"></activity>

        <!-- Service -->
        <service android:name="com.newx.service.net.CommunicationService"></service>

    </application>

</manifest>